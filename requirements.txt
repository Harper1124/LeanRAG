# Top-level LeanRAG dependencies.
# Keep transitive dependencies unpinned where possible so this environment can coexist
# with model-serving packages such as vLLM. Ollama itself is a separate service and
# does not need to be installed in this Python environment.

jieba>=0.42.1
milvus-lite>=2.5.1
numpy>=1.26
openai>=1.0
pandas>=2.0
pyarrow>=14.0
pyahocorasick>=2.0
pymilvus>=2.4
PyMySQL>=1.1
PyYAML>=6.0
requests>=2.31
scikit-learn>=1.4
tiktoken>=0.7
tqdm>=4.66
umap-learn>=0.5
